import { YDirective } from './y.directive';

describe('YDirective', () => {
  it('should create an instance', () => {
    const directive = new YDirective();
    expect(directive).toBeTruthy();
  });
});
